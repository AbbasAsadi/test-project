import 'package:flutter/material.dart';

class TabletMainWidget extends StatelessWidget {
  const TabletMainWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("TabletMainWidget"),
    );
  }
}
